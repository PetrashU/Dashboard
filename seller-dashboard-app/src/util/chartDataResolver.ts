import { ChartSettingsType } from "../component/widget/SalesChartWidg";
import { TIME_SCOPE } from "../data/constant";

export const chartDataResolver = (settings : ChartSettingsType) => {
    let data : any = [];
    if(settings.timeScope === TIME_SCOPE.DAY) {
        
    }
}